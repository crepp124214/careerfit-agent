# CareerFit Agent — 环境变量示例
# 复制为 .env 后按需修改

# 后端 API 基础地址（前端 Vite 用）
VITE_API_BASE_URL=http://localhost:8000/api

# 应用变体：frontend-only 时跳过后端探测，全部功能走 BackendNotReadyNotice
VITE_APP_VARIANT=frontend-only
